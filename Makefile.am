AUTOMAKE_OPTIONS = foreign
bin_PROGRAMS = funcexec
funcexec_SOURCES = main.c func.c func.h

clean-all: clean uninstall distclean
	rm -rf aclocal.m4 compile configure \
		depcomp install-sh missing \
		Makefile.in autom4te.cache

